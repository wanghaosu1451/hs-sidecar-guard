"""Computer Use 子系统：让 AI 在真实本机桌面上自主观察并操作。

设计目标（对齐 Claude Code 甚至更多）：
- 无视觉模型也能“看屏”：把窗口/UI 控件抽成文本快照（元素名 + 类型 + 坐标），
  让 qwen 等纯文本模型能据此规划下一步。
- 真实操作：鼠标点击/移动、键盘输入/快捷键、打开应用、列/切窗口、打开网页。
- 安全边界：全部操作仅限当前登录会话桌面；高危（删/改系统）仍走 run_shell 的
  不可逆护栏，不在此绕过。
"""